<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Notes App</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>
<body>
    <div class="container mt-2 mb-4">
        <h2>user: <span class="text-primary"><%= user.username %></span></h2>
        <h5>email: <span class="text-muted"><%= user.email%></span></h5>
        <hr>
    </div>
    <div class="container mb-4">
        <h2>Subject: <span class="text-primary"><%= subjectNotes.name %></span></h2>
    </div>

    <div class="container">
        <a href="/main/<%=user.username%>" class="btn btn-secondary text-capitalize">go back</a>
    </div>

    <% if(!subjectNotes.notes.length){ %>
        <div class="container px-4 py-2 mt-3 mb-4">
            <h5 class="text-capitalize text-muted">no new notes...</h5>
        </div>
    <% }else{ %>
        <div class="container px-4 py-2 mt-3 mb-4">
            <% subjectNotes.notes.forEach(note => { %>
                <div class="container card border-success mt-3 mb-3 d-flex flex-row justify-content-between p-3 align-items-center">
                    <div>
                        <h4 class="card-title text-capitalize"><%=note.title%></h4>
                    </div>
                    <div class="text-capitalize">
                        <a href="/main/<%=user.username%>/<%=subjectNotes.name%>/<%=note.slug%>" class="btn btn-primary">read note</a>
                        <a href="/main/<%=user.username%>/<%=subjectNotes.name%>/<%=note.slug%>/edit" class="btn btn-info text-light">edit note</a>
                        <form action="/note/<%=user.username%>/<%=subjectNotes.name%>/<%=note.slug%>?_method=DELETE" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger">delete</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } %>

</body>
</html>